# AWS ECS + RDS Deployment using Terraform

## Project Overview
This project deploys a web application using:

- ECS Fargate
- RDS PostgreSQL (Free-tier)
- VPC with public/private subnets
- Security groups for secure access
- Application Load Balancer

## Commands

terraform init  
terraform validate  
terraform plan  
terraform apply  
